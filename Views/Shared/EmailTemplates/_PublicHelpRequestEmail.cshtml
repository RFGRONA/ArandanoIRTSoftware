@model ArandanoIRT.Web._1_Application.DTOs.Admin.PublicHelpRequestDto

<!DOCTYPE html>
<html lang="es">
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            color: #333;
        }

        .container {
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            max-width: 600px;
        }

        .header {
            background-color: #f8f9fa;
            padding: 10px;
            border-bottom: 1px solid #ddd;
        }

        ul {
            list-style: none;
            padding: 0;
        }

        li {
            margin-bottom: 10px;
        }

        b {
            color: #0056b3;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="header">
        <h3>Nueva Solicitud de Ayuda (Pública)</h3>
    </div>
    <h4>Asunto: @Model.Subject</h4>
    <hr>
    <p>Se ha recibido una nueva solicitud de ayuda de un visitante no autenticado:</p>
    <ul>
        <li><b>Nombre:</b> @Model.Name</li>
        <li><b>Email de Contacto:</b> @Model.ContactEmail</li>
    </ul>
    <p><b>Mensaje:</b></p>
    <p>@Model.Message</p>
</div>
</body>
</html>