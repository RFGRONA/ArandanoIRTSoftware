@*
    Página dedicada a cambiar el estado de una planta.
    Permite al usuario seleccionar un nuevo estado de una lista y, de manera
    crucial, registrar una observación o justificación para dicho cambio.
    Esto asegura que exista un historial claro de por qué y cuándo cambió
    el estado de una planta.
*@
@using ArandanoIRT.Web._0_Domain.Enums
@model ArandanoIRT.Web._1_Application.DTOs.Plants.PlantStatusUpdateDto
@{
    ViewData["Title"] = "Cambiar Estado de Planta";
    Layout = "_AdminLayout";
}

@section Styles {
    <link rel="stylesheet" href="~/css/crud-forms.css" asp-append-version="true"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Estilo para el textarea autoajustable */
        textarea.auto-grow {
            resize: none;
            overflow-y: hidden;
        }
    </style>
}

<div class="page-header">
    <h1>@ViewData["Title"]</h1>
    <h4>Planta: @Model.PlantName</h4>
</div>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <form asp-action="Change" method="post">
            @* Campo oculto que envía el ID de la planta para que el controlador sepa qué registro actualizar. *@
            <input type="hidden" asp-for="PlantId"/>
            <div class="card form-card">
                <div class="card-body">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="mb-3">
                        <label asp-for="NewStatus" class="form-label">Nuevo Estado</label>
                        <select asp-for="NewStatus" class="form-select" asp-items="ViewBag.AvailableStatuses">
                            <option value="">-- Seleccione un nuevo estado --</option>
                        </select>
                        <span asp-validation-for="NewStatus" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Observation" class="form-label"></label>
                        @* Este textarea se expande verticalmente a medida que se escribe, gracias al script de abajo. *@
                        <textarea id="observation-textarea" asp-for="Observation" class="form-control auto-grow" rows="4" placeholder="Añade una observación o justificación para el cambio de estado..."></textarea>
                        <span asp-validation-for="Observation" class="text-danger"></span>
                    </div>
                </div>
                <div class="card-footer">
                    <a asp-controller="Plants" asp-action="Details" asp-route-id="@Model.PlantId" class="btn btn-secondary-outline">Cancelar</a>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save me-2"></i>Guardar Cambio
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    <script>
        @*
            Script para la funcionalidad "auto-grow" del textarea.
            Ajusta dinámicamente la altura del campo de texto para que coincida
            con la altura de su contenido a medida que el usuario escribe,
            mejorando la experiencia de usuario.
        *@
        document.addEventListener('DOMContentLoaded', function () {
            const textarea = document.getElementById('observation-textarea');

            if (textarea) {
                const adjustHeight = () => {
                    textarea.style.height = 'auto'; // Resetea la altura para recalcular
                    textarea.style.height = (textarea.scrollHeight) + 'px'; // Ajusta la altura al contenido
                };

                // Llama a la función cuando el usuario escribe
                textarea.addEventListener('input', adjustHeight);

                // Ajusta la altura inicial por si hay contenido precargado
                adjustHeight();
            }
        });
    </script>
}