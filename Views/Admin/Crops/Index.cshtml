@*
    Página de índice para la gestión de cultivos.
    Muestra una tabla con un listado de todos los cultivos registrados en el sistema.
    Proporciona acciones CRUD básicas para cada cultivo: Editar, ver Detalles y Eliminar,
    así como un botón para crear un nuevo cultivo.
*@
@model IEnumerable<ArandanoIRT.Web._1_Application.DTOs.Crops.CropSummaryDto>
@{
    ViewData["Title"] = "Gestión de Cultivos";
    Layout = "_AdminLayout";
}

@section Styles {
    <link rel="stylesheet" href="~/css/management.css" asp-append-version="true"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
}

<div class="page-header">
    <h1>@ViewData["Title"]</h1>
    <div class="header-actions">
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus me-2"></i>Crear Nuevo Cultivo
        </a>
    </div>
</div>

@* Muestra notificaciones de éxito o error después de realizar una acción (ej. crear, editar, eliminar). *@
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}
@if (ViewData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">@ViewData["ErrorMessage"]</div>
}

<div class="card table-card">
    <div class="table-responsive">
        <table class="table">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Ciudad</th>
                    <th>Fecha de Creación</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
            @if (Model != null && Model.Any())
            {
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.Name)</td>
                        <td>@Html.DisplayFor(modelItem => item.CityName)</td>
                        <td>@Html.DisplayFor(modelItem => item.CreatedAt)</td>
                        <td>
                            @* Grupo de botones de acción para cada registro de la tabla. *@
                            <div class="action-buttons">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn-action edit" title="Editar">
                                    <i class="fas fa-pencil-alt"></i>
                                </a>
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn-action details" title="Detalles">
                                    <i class="fas fa-info-circle"></i>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn-action delete" title="Eliminar">
                                    <i class="fas fa-trash-alt"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            }
            else
            {
                @* Mensaje que se muestra si no hay ningún cultivo registrado en la base de datos. *@
                <tr>
                    <td colspan="4" class="text-center py-4">No hay cultivos registrados.</td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>