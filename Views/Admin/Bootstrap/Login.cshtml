@*
    Página de "Login de Arranque" (Bootstrap Login).
    Esta vista se utiliza para un escenario de configuración inicial: el inicio de
    sesión del primer superadministrador cuando la base de datos está vacía o
    no existen otros usuarios administradores. Permite la creación y autenticación
    inicial para configurar el sistema por primera vez.
*@
@model ArandanoIRT.Web._1_Application.DTOs.Admin.AdminLoginDto
@{
    Layout = null; @* Página de autenticación independiente que no utiliza un layout principal. *@
    ViewData["Title"] = "Bootstrap Login";
}

<!DOCTYPE html>
<html>
<head>
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css"/>
</head>
<body class="bg-light">
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-4" style="margin-top: 15vh;">
            <div class="card">
                <div class="card-body">
                    <h3 class="card-title text-center">Acceso de Arranque</h3>
                    <form asp-action="Login" method="post">
                        @* Muestra errores de validación que no están asociados a un campo específico del modelo. *@
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="mb-3">
                            <label asp-for="Username" class="form-label"></label>
                            <input asp-for="Username" class="form-control"/>
                            <span asp-validation-for="Username" class="text-danger"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Password" class="form-label"></label>
                            <input asp-for="Password" class="form-control"/>
                            <span asp-validation-for="Password" class="text-danger"></span>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Entrar</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>